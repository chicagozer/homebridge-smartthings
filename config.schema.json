{
  "pluginAlias": "HomeBridgeSmartThings",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Smartthings Plug (JM)"},
      "BaseURL": {
        "title": "Base URL",
        "type": "string",
        "required": true,
        "default": "https://api.smartthings.com/v1/"},
      "AccessToken": {
        "title": "Access Token",
        "type": "string",
        "required": true,
        "default": ""},
      "GarageDoorMaxPoll": {
        "title": "Garage Door Max Poll",
        "type": "integer",
        "required": true,
        "default": 40},
      "pollingInterval": {
        "title": "Polling interval (msec) for lock",
        "type": "integer",
        "required": true,
        "default": 1000},
      "IgnoreLocations": {
        "title": "Locations to Ignore",
        "type": "array",
        "items": {
          "title": "Location Name",
          "type": "string"
        }
      }
    }
  },
  "form": null,
  "display": null
}
